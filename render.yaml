services:
  - type: web
    name: igowia-chat
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: GROQ_API_KEY
        sync: false
      - key: ADMIN_CODE
        sync: false
      - key: NODE_ENV
        value: production
